def sum_numbers(n:int):
    result = 0    # Initialize the variable 'final_sum' and set it to 0 initially
    for i in range(1,n + 1):       # Use a loop to iterate over numbers from 0 to n (inclusive)
       result=result+i
    print(f'sum_numbers of {n} is {result}')
n=5
sum_numbers(n)
